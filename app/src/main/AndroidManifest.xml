<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ScanQ"
        android:theme="@style/Theme.ScanQ"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".auth.StudentLoginActivity" android:exported="false" />
        <activity android:name=".auth.StudentRegisterActivity" android:exported="false" />
        <activity android:name=".auth.TutorLoginActivity" android:exported="false" />
        <activity android:name=".auth.TutorRegisterActivity" android:exported="false" />
        <activity android:name=".student.StudentDashboardActivity" android:exported="false" />
        <activity android:name=".tutor.TutorDashboardActivity" android:exported="false" />

    </application>
</manifest>
